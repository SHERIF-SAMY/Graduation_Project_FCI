Extract ONLY explicitly mentioned rental product attributes from the user message.

Fields: category, brand, location, max_price, condition (New/Used), name_keyword

CRITICAL RULES:
- ONLY extract a field if the user CLEARLY and EXPLICITLY mentioned it.
- If the user did NOT mention a location, set location to null. Do NOT guess or infer locations.
- If the user did NOT mention a condition (new/used), set condition to null. Do NOT guess.
- If the user did NOT mention a brand, set brand to null.
- If the user did NOT mention a price, set max_price to null.
- Arabic words like "على", "بنور", "عايز", "بدور", "محتاج" are NOT locations or conditions — ignore them.

Translation rules (output values in English):
- Product names/keywords: "لابتوب"/"لابتوبات" → "laptop", "كاميرا"/"كاميرات" → "camera", "دراجة" → "bike", "موبايل" → "mobile", "طابعة" → "printer", "بروجيكتور" → "projector", "سماعة" → "headphone", "عجلة" → "bike", "تلفزيون" → "TV"
- Always use SINGULAR form for name_keyword: "laptops" → "laptop", "cameras" → "camera"
- Locations (only if user explicitly said a place): "المعادي" → "Maadi", "مدينة نصر" → "Nasr City", "الزمالك" → "Zamalek", "الدقي" → "Dokki", "الإسكندرية" → "Alexandria", "هليوبوليس" → "Heliopolis"
- Condition: "جديد"/"جديدة"/"new" → "New"; "مستعمل"/"مستخدم"/"used" → "Used"
- Do NOT set category when you have a name_keyword — set category to null.

User message: "{user_query}"

Reply with ONLY a valid JSON object. No explanation.
If a field is not explicitly mentioned, set it to null.
Example: {{"category": null, "brand": null, "location": null, "max_price": null, "condition": null, "name_keyword": "laptop"}}
