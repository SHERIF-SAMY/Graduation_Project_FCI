# 🏪 RentHub AI - Graduation Project Presentation

> **ملحوظة للمتحدث:** هذه البريزنتيشن مصممة لتستغرق **5 دقائق كحد أقصى**. كل نقطة مكتوبة بشكل مباشر ومختصر (Bullet points) لتسهيل الشرح والشعور بالثقة أثناء المناقشة.

---

## ⏱️ (0:00 - 0:30) مقدمة (Introduction)
- **المشروع:** RentHub AI - مساعد ذكي لمنصة تأجير منتجات.
- **الهدف الرئيسي:** تحويل تجربة المستخدم التقليدية المليئة بالخطوات (البحث، الفلترة، الحجز) إلى **محادثة طبيعية بسيطة** وكأنك بتكلم بائع حقيقي.
- **التحدي:** تقديم نظام سريع، آمن، وبيدعم اللغة العربية (بلهجة مصرية) والإنجليزية.
- **الـ 3 ركائز الأساسية للـ AI:** 
  1. Smart Search
  2. End-to-End Chat Agent
  3. Personalized Recommendation System

---

## 🔍 (0:30 - 2:00) Feature 1: Smart Search
**كيف يفهم النظام طلبات المستخدم المعقدة ويحولها لنتائج دقيقة؟**

- **المشكلة (Problem):** البحث التقليدي بالكلمات المفتاحية (Keywords) بيفشل لو المستخدم كتب "عايز لابتوب ديل رخيص في مدينة نصر". الطرق التقليدية مش بتفهم السياق.
- **الحل (Solution):** استخدام LLM (Llama 3.1) لاستخراج الكيانات (Entity Extraction) وتحويل النص المكتوب أياً كانت لغته لمعايير بحث مفهومة (فئة، ماركة، سعر، حالة، وموقع).
- **الهيكلة (Architecture):**
  1. **Entity Extractor:** بيفهم الطلب ويستخرج الـ Filters.
  2. **SQL Builder:** بيبني Query ديناميكي بناءً على الـ Entities.
  3. **Custom Ranker:** بيرتب النتائج (Top 5) بناءً على وزن الكلمات والسعر، عشان نعرض للمستخدم أنسب حاجة ليه.
  4. **Query Cache:** نظام Caching ذكي بيحفظ الردود للطلبات المتشابهة لتسريع الرد (أقل من 50ms).
- **طبقة الحماية (Security Layer):**
  - **No SQL Injection:** بنستخدم Parameterized Queries عبر `SQLAlchemy`.
  - **Read-Only View:** الـ AI ملوش صلاحية يشوف الداتا الأساسية، بيقرأ فقط من `Products_LLm` VIEW آمن مفيش فيه بيانات حساسة.

---

## 🤖 (2:00 - 3:30) Feature 2: End-to-End Chat Agent
**إزاي الـ AI بيقدر يكمل دورة حجز كاملة لوحده؟**

- **المشكلة (Problem):** إجبار المستخدم يخرج من الشات عشان يعمل حجز بيقلل نسبة الـ Conversion rate وبيضيع وقت.
- **الحل (Solution):** بناء Agentic Booking System (بناء على State Machine) بيشتغل كبائع بياخد الطلب، يسأل عن التفاصيل الناقصة (التواريخ، العنوان)، ويأكد الحجز مباشرة.
- **الهيكلة (Architecture):**
  1. **Parallel Processing:** بيتم تشغيل `IntentAgent` (لفهم النية: هل هو بحث ولا حجز ولا إلغاء؟) مع `EntityExtractor` في نفس الوقت لتقليل وقت الرد.
  2. **Booking Context (Memory):** النظام بيحفظ حالة المستخدم (مثلاً: AWAITING_DATES) في الـ RAM عشان يعرف الخطوة الجاية.
  3. **.NET API Proxy:** لما المستخدم يأكد الحجز، بايثون بيبعت Request للـ .NET Backend عن طريق Proxy.
- **طبقة الحماية (Security Layer):**
  - **Zero Write Access:** الـ AI بايثون **مستحيل** يكتب أو يعدل في الـ Database مباشرة. كل عمليات الحجز والإلغاء بتتم من خلال الـ .NET API.
  - **Auth Token Forwarding:** بيتم تمرير الـ JWT Token بتاع المستخدم للـ .NET API لضمان إن المستخدم بيحجز لنفسه فقط، وتفادي مشاكل الـ CORS.

---

## 🎯 (3:30 - 4:30) Feature 3: Personalized Recommendation System
**إزاي بنعرض لكل مستخدم منتجات مخصصة ليه هو بس؟**

- **المشكلة (Problem):** الـ Cold Start المشهورة (المستخدم الجديد نعرضله إيه؟) وعرض منتجات مش مناسبة لاهتمامات المستخدم الحالي.
- **الحل (Solution):** نظام هجين (Hybrid Recommendation) بيتعلم من تفاعلات المستخدم.
- **الهيكلة (Architecture):**
  1. **Interaction Logger:** بيسجل بهدوء (Asynchronously) أي بحث أو ضغطة يعملها المستخدم في جدول `UserInteractions`.
  2. **Preference Builder:** بيعمل Profile للمستخدم بيحسب الـ Time-decay weighting (التفاعلات الأحدث ليها وزن أكبر).
  3. **Candidate Ladder:** لو الـ Profile مش واضح (Cold Start)، بنعرض الـ Trending products من الـ `ProductStats`. لو واضح، بنعمل Scoring بناءً على توافق الماركة، السعر، والمكان (+4 نقاط للكلمة، +5 للماركة... إلخ).
- **طبقة الحماية (Security Layer):**
  - **Fire-and-forget:** تسجيل البيانات بيتم في Background Tasks عشان ميأثرش على سرعة الـ API ولا يعطل الـ Request الأساسي.
  - **Data Isolation:** محرك التوصيات بيتعامل مع أرقام و Logs بدون ما يحتاج يوصل لأي بيانات شخصية حساسة للمستخدم.

---

## 🚀 (4:30 - 5:00) الخاتمة (Conclusion)
- **High Performance:** قدرنا نوصل لـ Pipeline معقد جداً لكن وقت الاستجابة بالكامل تحت الـ 2 ثانية (وفي البحث المكرر تحت الـ 50 مللي ثانية).
- **Secure by Design:** فصل كامل بين الـ AI والـ Database Writing.
- **Scalable:** نقدر نضيف Intents و Agents جديدة بسهولة بدون كسر النظام.

**شكراً ليكم ومستعدين لأي أسئلة!**
